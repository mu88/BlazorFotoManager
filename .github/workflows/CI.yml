name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0'
    - name: Install global tool ElectronNET.CLI
      run: dotnet tool install ElectronNET.CLI -g
    - name: Restore dependencies
      run: dotnet restore FotoManager.sln
    - name: Build
      run: dotnet build --no-restore FotoManager.sln
    - name: Test
      run: dotnet test --collect:"XPlat Code Coverage" --no-build FotoManager.sln /p:Exclude="[*]FotoManager.Pages.*"
    - name: Upload code coverage report
      uses: codecov/codecov-action@v2
    - name: Build Windows app with ElectronNET
      run: |
        cd FotoManager
        electronize build /target win